import { MovimientoPipe } from './movimiento.pipe';

describe('MovimientoPipe', () => {
  it('create an instance', () => {
    const pipe = new MovimientoPipe();
    expect(pipe).toBeTruthy();
  });
});
